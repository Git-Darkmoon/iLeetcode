// Write a JavaScript program to find the maximum number in an array.

const numbers = [341, 121, 13, 34, -5, -46, 71, 85, 9, 210]

const maxNumber = (array) => Math.max(...array)

console.log(maxNumber(numbers))
